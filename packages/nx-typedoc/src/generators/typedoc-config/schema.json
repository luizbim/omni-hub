{
  "$schema": "https://json-schema.org/schema",
  "$id": "TypeDocProjectConfig",
  "title": "",
  "type": "object",
  "properties": {
    "projects": {
      "type": "string",
      "description": "The NX Project to add the TypeDoc config to",
      "x-prompt": "What project would you like to add the TypeDoc config to?",
      "x-dropdown": "projects"
    },
    "outputPath": {
      "type": "string",
      "description": "The output path for the TypeDoc documentation",
      "$default": "docs",
      "x-prompt": "What is the output path for the TypeDoc documentation?"
    } 
   },
  "required": ["projects", "allProjects"]
}
